#calculate the growth advantage from the coefficients of two models
library(mnlpred)
library(nnet)
library(MASS)


logspace <- function(clademod, pangomod)

function(clademod, pangomod, coeff){
  
  
  
  
}